import ReactDOM from 'react-dom';
import './index.css';
import App from './App';

// We don't render too many components here, we only render once for our root component
ReactDOM.render(<App/>, document.getElementById("root"));
